-- Interview Preparation Platform - MySQL Schema Reference
-- Tables are auto-created by JPA (spring.jpa.hibernate.ddl-auto=update)
-- Use this script for manual setup or documentation

CREATE DATABASE IF NOT EXISTS interview_prep_db;
USE interview_prep_db;

-- Roles, Users (managed by JPA entities)
-- programming_questions, interview_questions, mock_tests, test_results
-- resumes, analytics, friends, friend_requests
-- chat_messages, group_chats, group_members, notifications, admin_reports
